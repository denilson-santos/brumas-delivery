<nav class="navbar topnav navbar-expand-lg">
    <div id="navbarNav" class="container collapse navbar-collapse">
        <ul class="navbar-nav">
            <li class="nav-item menu-border">
                <a class="nav-link menu-border-active" href="{{ BASE_URL }}">{{ iniDicionary['HOME'] }}</a>
            </li>
            <li class="nav-item menu-border">
                <a class="nav-link" href="{{ BASE_URL }}/contact">{{ iniDicionary['CONTACT'] }}</a>
            </li>
        </ul>
        <ul class="navbar-nav">
            <li class="nav-item dropdown dropdown-languages">
                <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
                    {% if iniDicionary['LANGUAGE'] == "English" %}
                        <img class="border border-white mb-1 mr-1" width="22px" src="/media/flags/usa.png">
                    {% else %}
                        <img class="border border-white mb-1 mr-1" width="22px" src="/media/flags/brazil.png">
                    {% endif %}
                    {{ iniDicionary['LANGUAGE'] }}
                <span class="caret"></span></a>
                <ul class="dropdown-menu">
                    <li data-lang="en">
                        <a class="dropdown-item" href="javascript:void(0)">
                            <img class="mb-1 mr-1" width="22px" src="/media/flags/usa.png">
                            English
                        </a>
                    </li>
                    <li data-lang="pt-br">
                        <a class="dropdown-item" href="javascript:void(0)">
                            <img class="mb-1 mr-1" width="22px" src="/media/flags/brazil.png">
                            Português
                        </a>
                    </li>
                </ul>
            </li>

            {% if userIsLogged() %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
                        <img alt="image" src="{{ userLogged['image'] ? userLogged['image'] : '/media/default/users/user-img-default-150x150.jpg' }}" class="rounded-circle mr-1" width="30">
                        <div class="d-sm-none d-lg-inline-block">{{ userLogged['first_name'] ~ ' ' ~ userLogged['last_name'] }}</div>
                    </a>

                    <div class="dropdown-menu">
                        <a class="dropdown-item" href="{{ route('name.profile') }}">
                            <i class="fas fa-user-alt"></i>
                            Perfil
                        </a>
                        
                        {% if userIsAuthorized(['admin', 'partner']) %}
                            <a class="dropdown-item" href="{{ route('name.restaurant-details') }}">
                                <i class="fas fa-utensils"></i>
                                Restaurante
                            </a>
                        {% endif %}

                        <a class="dropdown-item" href="{{ route('name.orders') }}"> 
                            <i class="fas fa-list-alt"></i>
                            Pedidos
                        </a>

                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="{{ route('name.logout') }}">
                          <i class="fas fa-sign-out-alt"></i> 
                          Sair
                        </a>
                    </div>
                </li>   
            {% else %}
                <li class="nav-item menu-border">
                    <a class="nav-link" href="{{ route('name.login') }}">{{ iniDicionary['LOGIN'] }}</a>
                </li>

                <li class="nav-item menu-border">
                    <a class="nav-link" href="{{ route('name.register-customer') }}">{{ iniDicionary['REGISTER'] }}</a>
                </li>

                <li class="nav-item menu-border">
                    <a class="nav-link" href="{{ route('name.register-partner') }}">{{ iniDicionary['BEAPARTNER'] }}</a>
                </li>
            {% endif %}

        </ul>
    </div>			
</nav>

<header>
    <div class="container">
        <div class="row">
            <div class="col-sm-3 logo">
                <a href="{{ BASE_URL }}"><img title="Brumas Delivery" src="/assets/images/logo-brumas-delivery-red.png" /></a>
            </div>
            <div class="col-sm-6 block-header-2">
                <div class="head-help ml-n5"><i class="fas fa-phone-alt"></i>(77) 9999-9999</div>
                <div class="head-email"><i class="fas fa-envelope"></i>contato@<span>brumasdelivery.com.br</span></div>
                
                <div class="search-area">
                    <input type="text" name="search" required placeholder="{{ iniDicionary['SEARCHFORANITEM'] }}" value="{{(notEmpty(filtersSelected['searchTerm'])? filtersSelected['searchTerm'] : '') }}"/>
            
                    <input type="submit" value="" class="btn-primary" id="search" />
                </div>
            </div>
            <div class="col-sm-3">
                <a href="{{ BASE_URL }}/cart">
                    <div class="cart-area">
                        <div class="cart-icon">
                            <div class="cart-quantity">9</div>
                        </div>
                        <div class="cart-total">
                            {{ iniDicionary['CART'] }}<br/>
                            <span>R$ 999,99</span>
                        </div>
                    </div>
                </a>
            </div>
        </div>
    </div>
</header>